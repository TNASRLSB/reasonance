# tests/field/scenarios/security.yaml
- id: security_53
  name: Workspace trust
  suite: security
  steps:
    - action: checkpoint
      label: workspace_trust
    - action: assert_no_new_errors

- id: security_54
  name: Permission deny
  suite: security
  steps:
    - action: checkpoint
      label: permission_deny
    - action: assert_no_new_errors

- id: security_55
  name: Policy file regex
  suite: security
  steps:
    - action: checkpoint
      label: policy_file_regex
    - action: assert_no_new_errors

- id: security_56
  name: Symlink escape
  suite: security
  steps:
    - action: checkpoint
      label: symlink_escape
    - action: assert_no_new_errors

- id: security_57
  name: Prompt injection resistance
  suite: security
  steps:
    - action: checkpoint
      label: prompt_injection
    - action: assert_no_new_errors
