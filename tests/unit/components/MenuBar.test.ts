import { describe, it } from 'vitest';

describe('MenuBar', () => {
  it.todo('renders all top-level menu labels (File, Edit, View, Terminal, Git, Help)');
  it.todo('menu dropdown is hidden by default');
  it.todo('clicking a menu trigger opens its dropdown');
  it.todo('clicking a second menu trigger closes the first and opens the second');
  it.todo('pressing Escape closes the open menu');
  it.todo('clicking outside the menu bar closes the open menu');
  it.todo('clicking a menu item with an action invokes the action and closes the menu');
  it.todo('hovering over a menu trigger switches the open menu when another is already open');
  it.todo('menu items with submenus show the submenu arrow indicator');
});
