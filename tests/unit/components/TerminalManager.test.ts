import { describe, it } from 'vitest';

describe('TerminalManager (P3 — mount/unmount only, xterm needs real DOM)', () => {
  it.todo('mounts without throwing');
  it.todo('unmounts cleanly without leaking event listeners');
});
